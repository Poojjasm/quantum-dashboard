# Copy this to .env.local for local development overrides.
# In production, set VITE_API_URL in your Vercel project settings.

# The Railway backend URL — replace with your actual Railway app URL after deployment
VITE_API_URL=https://your-railway-app.railway.app
